export interface I{{operationId}}Parameters{
  {{#each allParameters}}
		{{variableName this}}: {{variableType this.type}}; //{{this.description}} - eg. {{this.example}} (found in {{this.location}})
	{{/each}}
}
