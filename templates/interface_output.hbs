export interface I{{operationId}}Response{
	 {{#each responses}}
      //{{this.code}} - {{this.description}}
      {{#each data}}
      {{this.name}}: {{{variableType this.type}}};
      {{/each}}
      
   {{/each}}
}
